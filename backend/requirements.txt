# Core FastAPI & Server
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart==0.0.9

# Database & Async
motor==3.3.1
pymongo==4.5.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pyjwt==2.10.1
cryptography==42.0.8

# Pydantic & Validation
pydantic==2.6.4
pydantic-settings==2.0.0
email-validator==2.2.0

# Environment & Configuration
python-dotenv==1.0.1
tzdata==2024.2

# HTTP Requests & OAuth
requests==2.31.0
requests-oauthlib==2.0.0
aiohttp==3.8.6

# Scheduling
apscheduler==3.10.4

# Data Processing & Analysis
pandas==2.2.0
numpy==1.26.4
beautifulsoup4==4.12.3
lxml==4.9.4

# PDF Processing (consolidated to pdfplumber only)
pdfplumber==0.9.0

# Web Automation (using Playwright only - more modern than Selenium)
playwright==1.40.0

# Cloud Services
boto3==1.34.129
sendgrid==6.10.0

# Emergent Integration
emergentintegrations

# Development Tools (only in dev/test)
# pytest==8.0.0
# black==24.1.1  
# isort==5.13.2
# flake8==7.0.0
# mypy==1.8.0

# Removed redundant packages:
# - PyPDF2 (replaced by pdfplumber) 
# - tabula-py (specialized, can be added if needed)
# - selenium (replaced by playwright)
# - jq (not needed for Python)
# - typer (CLI tool, not needed in server)
